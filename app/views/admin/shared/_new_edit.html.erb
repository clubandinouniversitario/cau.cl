<div class="row">
  <div class="col-xs-9">
    <h3 class="text-uppercase">
      <% action_t = I18n.t(params[:action]) unless action_t.present? %>
	  <%= action_t %> <%= I18n.t(params[:controller].split('/')[1].singularize) %>
	</h3>
  </div>
  <div class="col-xs-3">
    <%= link_to I18n.t('buttons.back'), :back, class: 'btn btn-default btn-block m-t-20' %>
  </div>
</div>
<div><%= render 'form' %></div>
