
  <%= render 'layouts/menu' %>

	<div class="container">
		<div class="pg-title">
			<h1> <%= @page.title %> </h1>
		</div>
		<div class="pg-content">
			<%= sanitize @page.content %>
		</div>
	</div>
